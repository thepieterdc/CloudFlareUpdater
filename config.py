cf_email = "user@domain.com"  # CloudFlare email address
cf_apikey = ""  # CloudFlare API key
hosts = {"mydomain.com": ["server1", "server2", "mail"]}  # the hosts among with the A record names to update
